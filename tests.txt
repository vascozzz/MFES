create ex := new ExchangeSystem()

create prod := new Product(mk_token("automovel"));
print ex.insertProduct(prod);

create ord1 := new Order(<BUY>, prod, {"cor" |-> mk_token("vermelho"), "cavalos" |-> mk_token(3000)}, 0, 20000);
create ord2 := new Order(<SELL>, prod, {"cor" |-> mk_token("vermelho"), "cavalos" |-> mk_token(3000), "marca" |-> mk_token("nissan")}, 10000, 18000);

print ex.insertOrder(ord1);
print ex.insertOrder(ord2);

print ex.matchOrder(ord1);
print ex.pickMatch(ord1, ord2);